import assert from "node:assert/strict";

// Implement the function to pass the test.

const add = (number) => {
  // Write the code only here.

  return number;
};

assert.deepEqual(add(1)(2)(3)(4)(5)(6)(7)(8)(9)(), 45);
assert.deepEqual(add(9)(), 9);
